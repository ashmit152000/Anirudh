import 'package:flutter/material.dart';

class CenterName extends StatelessWidget {
  const CenterName({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Anirudh\'s first lecture"));
  }
}
